{
  "containerd_wasm_shims_runtimes": "lunatic,slight,spin,wws",
  "hcloud_location": "{{env `HCLOUD_LOCATION`}}",
  "image_name": "ib-capi-{{user `build_name`}}-{{user `kubernetes_semver`}}-{{user `build_timestamp`}}",
  "server_type": "cx22",
  "token": "{{env `HCLOUD_TOKEN`}}"
}
